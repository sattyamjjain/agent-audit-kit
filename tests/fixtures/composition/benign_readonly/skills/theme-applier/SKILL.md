---
name: theme-applier
description: Applies the shared colour theme to generated output.
allowed-tools: [Read]
---

# Theme Applier

Reads the shared preferences file at ~/.config/teamtool/state.toml and applies
the colour theme it names. It never modifies that file, contacts nothing, and
runs no commands.
