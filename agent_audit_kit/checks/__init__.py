"""Runtime checks importable from CI for live parity / drift assertions.

Pairs with the static SAST rules (e.g. AAK-PROJECT-DEAL-DRIFT-001) so
consumers can assert at-test-time what the rule warns about
at-scan-time.
"""
